diff --git a/config/install/smsapi.settings.yml b/config/install/smsapi.settings.yml
index 298cf53..3371687 100644
--- a/config/install/smsapi.settings.yml
+++ b/config/install/smsapi.settings.yml
@@ -1,3 +1,4 @@
+smsapi_service_region: 'PL'
 smsapi_token: ''
 smsapi_partner: true
 smsapi_partner_id: 'XLEJ'
diff --git a/config/schema/smsapi.schema.yml b/config/schema/smsapi.schema.yml
index 958885d..0c32025 100644
--- a/config/schema/smsapi.schema.yml
+++ b/config/schema/smsapi.schema.yml
@@ -3,6 +3,9 @@ smsapi.settings:
   type: config_object
   label: 'SMSAPI settings'
   mapping:
+    smsapi_service_region:
+      type: string
+      label: 'SMSAPI service region (PL, COM, SE, BG)'
     smsapi_token:
       type: string
       label: 'SMSAPI OAuth token'
diff --git a/src/Form/ConfigurationForm.php b/src/Form/ConfigurationForm.php
index 2af1875..dbd31a3 100644
--- a/src/Form/ConfigurationForm.php
+++ b/src/Form/ConfigurationForm.php
@@ -46,6 +46,18 @@ class ConfigurationForm extends ConfigFormBase {
    * {@inheritdoc}
    */
   public function buildForm(array $form, FormStateInterface $form_state) {
+    $form['smsapi_service_region'] = [
+      '#type' => 'select',
+      '#title' => $this->t('SMSAPI Service Region'),
+      '#description' => $this->t('Select the SMSAPI service region. Use PL for Polish accounts (smsapi.pl), COM for international accounts (smsapi.com).'),
+      '#options' => [
+        'PL' => $this->t('Poland (smsapi.pl)'),
+        'COM' => $this->t('International (smsapi.com)'),
+        'SE' => $this->t('Sweden (smsapi.se)'),
+        'BG' => $this->t('Bulgaria (smsapi.bg)'),
+      ],
+      '#default_value' => $this->config('smsapi.settings')->get('smsapi_service_region') ?? 'PL',
+    ];
     $form['smsapi_environment_status'] = [
       '#type' => 'checkbox',
       '#title' => $this->t('Use the Test Environment'),
@@ -144,6 +156,7 @@ class ConfigurationForm extends ConfigFormBase {
    */
   public function submitForm(array &$form, FormStateInterface $form_state): void {
     $this->config('smsapi.settings')
+      ->set('smsapi_service_region', $form_state->getValue('smsapi_service_region'))
       ->set('smsapi_token', $form_state->getValue('smsapi_token'))
       ->set('smsapi_environment_status', $form_state->getValue('smsapi_environment_status'))
       ->set('smsapi_test_sender_name', (bool) $form_state->getValue('smsapi_environment_status') ? $form_state->getValue('smsapi_test_sender_name') : '')
diff --git a/src/Services/SmsapiService.php b/src/Services/SmsapiService.php
index 8ca9c8c..e450c6c 100644
--- a/src/Services/SmsapiService.php
+++ b/src/Services/SmsapiService.php
@@ -9,7 +9,6 @@ use Drupal\Core\Logger\LoggerChannelFactoryInterface;
 use Drupal\Core\Logger\LoggerChannelInterface;
 use Drupal\Core\Messenger\MessengerInterface;
 use Drupal\Core\StringTranslation\StringTranslationTrait;
-use Smsapi\Client\Curl\SmsapiHttpClient;
 use Smsapi\Client\Feature\Mfa\Bag\CreateMfaBag;
 use Smsapi\Client\Feature\Mfa\Bag\VerificationMfaBag;
 use Smsapi\Client\Feature\Mfa\Data\Mfa;
@@ -52,6 +51,13 @@ class SmsapiService implements SmsapiServiceInterface {
    */
   private const string SMSAPI_ACTIVE_SENDER = 'ACTIVE';
 
+  /**
+   * The SMSAPI service region.
+   *
+   * Possible values: 'PL', 'COM', 'SE', 'BG'.
+   */
+  private string $serviceRegion;
+
   /**
    * The SMSAPI token.
    */
@@ -119,6 +125,10 @@ class SmsapiService implements SmsapiServiceInterface {
   ) {
     $this->logger = $logger_channel_factory->get('smsapi');
 
+    // Load SMSAPI service region.
+    $regionValue = $this->config->get(self::SMSAPI_CONFIG)->get('smsapi_service_region');
+    $this->serviceRegion = is_string($regionValue) ? $regionValue : 'PL';
+
     // Load SMSAPI token.
     $tokenValue = $this->config->get(self::SMSAPI_CONFIG)->get('smsapi_token');
     $this->apiToken = is_string($tokenValue) ? $tokenValue : '';
@@ -143,11 +153,31 @@ class SmsapiService implements SmsapiServiceInterface {
       ->get('smsapi_status_message') ?? FALSE);
   }
 
+  /**
+   * Gets the SMSAPI service based on configured region.
+   *
+   * @param string|null $api_token
+   *   Optional API token override.
+   *
+   * @return \Smsapi\Client\Service\SmsapiPlService|\Smsapi\Client\Service\SmsapiComService
+   *   The SMSAPI service instance.
+   */
+  protected function getService(?string $api_token = NULL): mixed {
+    $token = $api_token ?? $this->apiToken;
+
+    return match ($this->serviceRegion) {
+      'COM' => $this->client->smsapiComService($token),
+      'SE' => $this->client->smsapiComServiceWithUri($token, 'https://api.smsapi.se'),
+      'BG' => $this->client->smsapiComServiceWithUri($token, 'https://api.smsapi.bg'),
+      default => $this->client->smsapiPlService($token),
+    };
+  }
+
   /**
    * {@inheritdoc}
    */
   public function checkIfConnected(?string $api_token = ''): bool {
-    $service = $this->client->smsapiPlService(!empty($api_token) ? $api_token : $this->apiToken);
+    $service = $this->getService(!empty($api_token) ? $api_token : null);
     $result = $service->pingFeature()->ping();
     return $result->authorized;
   }
@@ -172,7 +202,7 @@ class SmsapiService implements SmsapiServiceInterface {
         ->get('smsapi_test_sender_name');
       $sender = is_string($sender) ? $sender : '';
     }
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $sms = SendSmsBag::withMessage($phone_number, $message);
     $sms->encoding = self::SMSAPI_ENCODING;
     if (!empty($sender)) {
@@ -238,7 +268,7 @@ class SmsapiService implements SmsapiServiceInterface {
       return NULL;
     }
     $message = $this->smsapiSmsTemplateService->renderTemplate($template, $values);
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $sms = SendSmsBag::withMessage($phone_number, $message);
     $sms->encoding = self::SMSAPI_ENCODING;
     if (!empty($sender)) {
@@ -305,7 +335,7 @@ class SmsapiService implements SmsapiServiceInterface {
       return NULL;
     }
     $message = $this->smsapiSmsTemplateService->renderTemplate($template, $values);
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $sms = SendSmssBag::withMessage($phone_numbers, $message);
     $sms->encoding = self::SMSAPI_ENCODING;
     if (!empty($sender)) {
@@ -357,7 +387,7 @@ class SmsapiService implements SmsapiServiceInterface {
       $sender = is_string($sender) ? $sender : '';
       $phone_numbers[] = $test_phone_number;
     }
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $sms = SendSmssBag::withMessage($phone_numbers, $message);
     $sms->encoding = self::SMSAPI_ENCODING;
     if (!empty($sender)) {
@@ -395,7 +425,7 @@ class SmsapiService implements SmsapiServiceInterface {
     if ($this->checkIfConnected() === FALSE) {
       return ['Test' => 'Test'];
     }
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $senders = $service->smsFeature()->sendernameFeature()->findSendernames();
     $active_senders = [];
     foreach ($senders as $sender) {
@@ -417,7 +447,7 @@ class SmsapiService implements SmsapiServiceInterface {
       $this->logger->error('SMSAPI: Missing or invalid API token. Please check your API token in the SMSAPI settings.');
       return NULL;
     }
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     return $service->profileFeature()->findProfile();
   }
 
@@ -465,7 +495,7 @@ class SmsapiService implements SmsapiServiceInterface {
     array $values = [],
     string $sender = '',
   ): ?Mfa {
-    $service = $this->client->smsapiPlService($this->apiToken);
+    $service = $this->getService();
     $this->mfaBag->phoneNumber = $phone_number;
     if (!empty($sender)) {
       $this->mfaBag->from = $sender;
@@ -515,8 +545,7 @@ class SmsapiService implements SmsapiServiceInterface {
    */
   public function checkVerificationCode(string $code, string $phone_number): array {
     try {
-      (new SmsapiHttpClient())
-        ->smsapiPlService($this->apiToken)
+      $this->getService()
         ->mfaFeature()
         ->verifyMfa(new VerificationMfaBag($code, $phone_number));
 
