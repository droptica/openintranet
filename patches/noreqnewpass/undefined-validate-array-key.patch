diff --git a/modules/contrib/noreqnewpass/noreqnewpass.module b/modules/contrib/noreqnewpass/noreqnewpass.module
index xxxxxxx..xxxxxxx 100644
--- a/modules/contrib/noreqnewpass/noreqnewpass.module
+++ b/modules/contrib/noreqnewpass/noreqnewpass.module
@@ -15,8 +15,14 @@ function noreqnewpass_form_alter(&$form, FormStateInterface $form_state, $form_id)
     $config = \Drupal::config('noreqnewpass.settings_form');
 
     if ($config->get('noreqnewpass_disable')) {
-      $key = array_search('::validateFinal', $form['#validate']);
-      $form['#validate'][$key] = 'noreqnewpass_user_login_final_validate';
+      // Initialize #validate as an array if it doesn't exist
+      if (!isset($form['#validate'])) {
+        $form['#validate'] = [];
+      }
+      $key = array_search('::validateFinal', $form['#validate'], TRUE);
+      if ($key !== FALSE) {
+        $form['#validate'][$key] = 'noreqnewpass_user_login_final_validate';
+      }
     }
   }
 }
